---
import { Image } from 'astro:assets';
---

<section class="hero" id="inicio">
    <div class="hero-content">
        <div class="profile-info">
            <h1>José Antonio Francisco Mogollón</h1>
            <h2>Ingeniero en Sistemas Computacionales</h2>
            <div class="contact-info">
                <a href="mailto:joseantoniofranciscomogollon@gmail.com" class="contact-item">
                    <span class="icon">📧</span>
                    joseantoniofranciscomogollon@gmail.com
                </a>
                <span class="contact-item">
                    <span class="icon">📱</span>
                    +32 833 150 4578
                </span>
            </div>
        </div>
    </div>
</section>

<style>
    .hero {
        min-height: 90vh;
        display: flex;
        align-items: center;
        justify-content: center;
        background: linear-gradient(135deg, #1a1a1a 0%, #2a2a2a 100%);
        color: white;
        padding: 2rem;
    }

    .hero-content {
        max-width: 1200px;
        text-align: center;
    }

    .profile-info {
        animation: fadeIn 1s ease-out;
    }

    h1 {
        font-size: 3.5rem;
        margin: 0;
        background: linear-gradient(45deg, #64B5F6, #2196F3);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        margin-bottom: 0.5rem;
    }

    h2 {
        font-size: 1.8rem;
        color: #90CAF9;
        margin: 0;
        margin-bottom: 2rem;
        font-weight: 400;
    }

    .contact-info {
        display: flex;
        flex-direction: column;
        gap: 1rem;
        align-items: center;
        font-size: 1.1rem;
    }

    .contact-item {
        display: flex;
        align-items: center;
        gap: 0.5rem;
        color: #fff;
        text-decoration: none;
        transition: color 0.3s ease;
    }

    .contact-item:hover {
        color: #64B5F6;
    }

    .icon {
        font-size: 1.2rem;
    }

    @keyframes fadeIn {
        from {
            opacity: 0;
            transform: translateY(20px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    @media (max-width: 768px) {
        h1 {
            font-size: 2.5rem;
        }

        h2 {
            font-size: 1.5rem;
        }
    }
</style>
